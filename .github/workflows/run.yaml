name: learn-github-actions
on:
  [push]
jobs:
  add_text:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        # with:
        #   ref: develop
      - name: Push File
        run: |
          VERSION=$(<version.txt)
          echo "$((VERSION+1))" >> version.txt